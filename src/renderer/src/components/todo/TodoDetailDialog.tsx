import React from 'react'

// Placeholder; full detail dialog lands in Task 13.
export function TodoDetailDialog(_props: {
  itemId: string
  onClose: () => void
}): React.JSX.Element | null {
  return null
}
