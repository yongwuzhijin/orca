import React from 'react'

// Placeholder mounted by App.tsx; full kanban implementation lands in Task 12.
export default function TodoPage(): React.JSX.Element {
  return <div className="flex h-full w-full items-center justify-center text-muted-foreground" />
}
